services:
  taiko-rating:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GUNICORN_CMD_ARGS=--bind=0.0.0.0:5000 --workers=2 --timeout=120 --access-logfile=- --error-logfile=-
    restart: unless-stopped
